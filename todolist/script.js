const todoInput=document.querySelector('#todoInput');const addBtn=document.querySelector('#addBtn');const todoList=document.querySelector('#todoList');const todoCount=document.querySelector('#todoCount');const filterButtons=document.querySelectorAll('.filter');let todos=[];let currentFilter='all';addBtn.addEventListener('click',addTodo);todoInput.addEventListener('keydown',e=>{if(e.key==='Enter')addTodo()});filterButtons.forEach(b=>b.addEventListener('click',()=>{filterButtons.forEach(x=>x.classList.remove('active'));b.classList.add('active');currentFilter=b.dataset.filter;renderTodos()}));function addTodo(){const text=todoInput.value.trim();if(!text){alert('할 일을 입력해주세요.');return;}todos.push({id:Date.now(),text,completed:false});todoInput.value='';renderTodos()}function renderTodos(){todoList.innerHTML='';todos.filter(t=>currentFilter==='all'||(currentFilter==='active'?!t.completed:t.completed)).forEach(todo=>{const li=document.createElement('li');li.className='todo-item'+(todo.completed?' completed':'');li.innerHTML=`<input type="checkbox" ${todo.completed?'checked':''}><span>${todo.text}</span><button class="delete-btn">삭제</button>`;li.querySelector('input').onchange=()=>{todo.completed=!todo.completed;renderTodos()};li.querySelector('button').onclick=()=>{todos=todos.filter(x=>x.id!==todo.id);renderTodos()};todoList.appendChild(li)});todoCount.textContent=todos.filter(t=>!t.completed).length}